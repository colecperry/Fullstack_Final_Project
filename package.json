{
  "dependencies": {
    "bootstrap": "^5.3.0",
    "jquery": "^3.7.0",
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "start": "node server.js",
    "test": "jest",
    "build": "npm run build:firebase",
    "build:firebase": "react-scripts build --basepath client",
    "deploy": "firebase deploy"
  }
}
